const initialState = {
  plants: [],
};

const initialPlant = {
  id: "",
  nickname: "",
  species: "",
  h20frequency: null,
  image: "", //(optional)
};

const reducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
